<% if current_user %>
  <main class="home-hero-shell">
    <nav class="home-nav" aria-label="ホーム操作">
      <% if current_user.score_viewer? %>
        <%= link_to "チームスコア", admin_team_weekly_scores_path, class: "home-nav-btn" %>
      <% end %>
      <% if current_user.system_admin? %>
        <%= link_to "管理画面", rails_admin_path, class: "home-nav-btn" %>
      <% end %>
      <%= button_to "ログアウト", logout_path, method: :delete, class: "home-nav-btn" %>
    </nav>

    <% flash.each do |type, message| %>
      <div class="flash <%= type %> home-flash" data-controller="flash"><%= message %></div>
    <% end %>

    <section class="home-hero-left" aria-labelledby="home-copy-heading">
      <%= render "home_logo" %>

      <div class="home-copy-block">
        <div class="home-copy-inner">
          <% if @assignment %>
            <div class="home-copy-action">
              <div>
                <h1 id="home-copy-heading" class="home-copy-line home-copy-greeting">こんばんは。</h1>
                <p class="home-copy-line home-copy-sub">今週のあなたの声を</p>
                <p class="home-copy-line home-copy-sub">聞かせてください。</p>
              </div>
              <svg class="home-copy-pulse" viewBox="0 0 260 32" aria-hidden="true">
                <polyline points="0,16 16,16 22,16 29,5 36,27 43,16 58,16 64,10 70,22 76,16 102,16 112,16 120,4 128,28 136,16 154,16 162,8 170,24 178,16 206,16 214,16 222,6 230,26 238,16 260,16" />
              </svg>
            </div>
          <% else %>
            <h1 id="home-copy-heading" class="home-copy-line home-copy-greeting"><%= current_user.name %>さん</h1>
            <p class="home-copy-line home-copy-sub">今週の回答は</p>
            <p class="home-copy-line home-copy-sub">ありません。</p>
          <% end %>
        </div>
      </div>

      <div class="home-bottom-block">
        <% if @assignment %>
          <div class="home-meta">
            <span class="home-meta-key">回答期限</span>
            <span class="home-meta-val home-deadline-val"><%= @assignment.survey.end_at.strftime("%Y/%-m/%-d %H:%M") %></span>
          </div>
        <% else %>
          <div class="home-meta">
            <span class="home-meta-key">Status</span>
            <span class="home-meta-val">回答が必要なサーベイはありません</span>
          </div>
        <% end %>
      </div>
    </section>

    <section class="home-hero-right" aria-label="サーベイ情報">
      <% if @assignment %>
        <%= link_to new_survey_assignment_response_path(@assignment), class: "home-pulse-visual home-pulse-link", aria: { label: "#{@assignment.survey.title}に回答する" } do %>
          <div class="home-ring home-ring-3"></div>
          <div class="home-ring home-ring-2"></div>
          <div class="home-ring home-ring-1"></div>
          <div class="home-pulse-core"></div>
        <% end %>
      <% else %>
        <div class="home-pulse-visual home-pulse-static">
          <div class="home-ring home-ring-3"></div>
          <div class="home-ring home-ring-2"></div>
          <div class="home-ring home-ring-1"></div>
          <div class="home-pulse-core"></div>
        </div>
      <% end %>
    </section>

    <footer class="home-footer-note">
      <svg class="home-lock-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <rect x="5" y="11" width="14" height="10" rx="2" />
        <path d="M8 11V7a4 4 0 0 1 8 0v4" />
      </svg>
      あなたの回答は匿名で処理されます。個人は特定されません。
    </footer>
  </main>
<% else %>
  <main class="home-hero-shell home-hero-shell-login">
    <div class="home-background-pulse" aria-hidden="true">
      <svg class="home-background-wave home-background-wave-primary" viewBox="0 0 1200 220" preserveAspectRatio="none">
        <path d="M0 118 H126 L154 118 L176 78 L204 168 L236 118 H356 L384 118 L408 54 L440 190 L470 118 H602 L628 118 L652 84 L680 148 L706 118 H840 L868 118 L890 66 L920 176 L948 118 H1200" />
      </svg>
    </div>

    <% flash.each do |type, message| %>
      <div class="flash <%= type %> home-flash" data-controller="flash"><%= message %></div>
    <% end %>

    <section class="home-hero-left" aria-labelledby="login-copy-heading">
      <%= render "home_logo" %>

      <div class="home-copy-block">
        <div class="home-copy-inner">
          <h1 id="login-copy-heading" class="home-copy-line home-copy-greeting"><%= login_greeting %></h1>
          <p class="home-copy-line home-copy-sub">今週の声を</p>
          <p class="home-copy-line home-copy-sub">聞かせてください。</p>
          <div class="home-login-actions">
            <%= button_to "/auth/google_oauth2", method: :post, data: { turbo: false }, class: "google-login-button", form_class: "google-login-form" do %>
              <span class="google-login-icon" aria-hidden="true">
                <svg viewBox="0 0 18 18" focusable="false">
                  <path fill="#4285f4" d="M17.64 9.2c0-.64-.06-1.25-.16-1.84H9v3.48h4.84a4.14 4.14 0 0 1-1.8 2.72v2.26h2.92c1.7-1.57 2.68-3.88 2.68-6.62z" />
                  <path fill="#34a853" d="M9 18c2.43 0 4.47-.8 5.96-2.18l-2.92-2.26c-.8.54-1.84.86-3.04.86-2.34 0-4.33-1.58-5.04-3.7H.94v2.33A9 9 0 0 0 9 18z" />
                  <path fill="#fbbc05" d="M3.96 10.72A5.4 5.4 0 0 1 3.68 9c0-.6.1-1.18.28-1.72V4.95H.94A9 9 0 0 0 0 9c0 1.45.34 2.82.94 4.05l3.02-2.33z" />
                  <path fill="#ea4335" d="M9 3.58c1.32 0 2.5.45 3.44 1.35l2.58-2.58C13.46.9 11.43 0 9 0A9 9 0 0 0 .94 4.95l3.02 2.33C4.67 5.16 6.66 3.58 9 3.58z" />
                </svg>
              </span>
              <span>Google でログイン</span>
            <% end %>
            <% unless Rails.configuration.x.google_auth_configured || Rails.configuration.x.google_auth_mock_enabled || Rails.env.test? %>
              <p class="home-login-help">Google認証の設定が未完了です。`GOOGLE_CLIENT_ID` と `GOOGLE_CLIENT_SECRET` を設定してください。</p>
            <% end %>
            <% if Rails.env.development? && !Rails.configuration.x.google_auth_configured && !Rails.configuration.x.google_auth_mock_enabled %>
              <%= button_to "開発用ログイン", development_login_path, method: :post, class: "home-nav-btn home-dev-login-btn" %>
            <% end %>
          </div>
        </div>
      </div>
    </section>

    <section class="home-hero-right" aria-label="ログイン">
      <div class="home-pulse-visual home-pulse-static">
        <div class="home-ring home-ring-3"></div>
        <div class="home-ring home-ring-2"></div>
        <div class="home-ring home-ring-1"></div>
        <div class="home-pulse-core"></div>
      </div>
    </section>

    <footer class="home-footer-note">
      <svg class="home-lock-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
        <rect x="5" y="11" width="14" height="10" rx="2" />
        <path d="M8 11V7a4 4 0 0 1 8 0v4" />
      </svg>
      あなたの回答は匿名で処理されます。個人は特定されません。
    </footer>
  </main>
<% end %>
